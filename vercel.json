{
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "installCommand": "cd spa && npm install",
  "buildCommand": "cd spa && npm run build",
  "outputDirectory": "spa/dist"
}
